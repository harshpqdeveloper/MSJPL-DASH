# excel/

Drop the production WIP export here as `.xlsx` or `.xls`.

- The dashboard automatically uses whichever file has the most recent modification time.
- To update the dashboard, just add a new file or overwrite an existing one — no restart needed.
- You can keep old exports in this folder; only the newest one is used.
